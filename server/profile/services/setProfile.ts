import { HttpStatus } from "@packages/statusCodes/status";
import { redisClient } from "core/redisClient";
import { ProfileModel } from "core/profileSchema"

export const SetProfile = async () => {
  try {

  } catch (e) {

  }
}

